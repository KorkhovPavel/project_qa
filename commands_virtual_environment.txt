python -m venv *  - создать виртуальное окружение(вместо * название папки)
git clone * - клонирует из гита(вместо * ссылка)
deactivate - деактивация окружения
source */bin/activate - активировать окружение (вместо * название папки с окружением)
which python - указывает на активный инт.